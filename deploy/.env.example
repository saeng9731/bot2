# ============================================================
#  bot2(JackBot) 환경변수 템플릿
#  서버에 .env 파일로 저장 후 sh/*.sh 스크립트가 자동 로드합니다.
#  사용법: cp .env.example .env  → 값 수정
# ============================================================

# MySQL 접속 정보 (서버에 설치한 MySQL 기준)
DB_ID=bot
DB_PASSWD=비밀번호변경
DB_IP=localhost
DB_PORT=3306

# 키움증권 모의투자 계좌 (10자리)
IMI1_ACCOUNT=8128743211
IMI1_SIMUL_NUM=11
START_DAILY_BUY_LIST=20250804

# 키움증권 REST API 키 (cf.py 에 이미 있으나 .env로 우선 적용 가능)
KIWOOM_API_KEY=QdDdAw4X_7uf4zIdDKuHV-Dkqxv1MsLvPBc7aQzKrdk
KIWOOM_API_SECRET=FgEBNFri25s6hqtwXIg067pg8TkPGo2mzfaVa_tSeYA
KIWOOM_IS_MOCK=True

# ETF 사용 여부 (False 유지 권장)
USE_ETF=False

# API 호출 최대 횟수 (기본 99999)
MAX_API_CALL=99999
